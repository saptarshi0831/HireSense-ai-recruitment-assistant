services:
  - type: web
    name: hiresense-backend
    env: python

    buildCommand: |
      pip install -r requirements.txt
      python -m spacy download en_core_web_sm

    startCommand: |
      uvicorn main --host 0.0.0.0 --port $PORT